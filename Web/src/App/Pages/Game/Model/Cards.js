export const Cards = () => {
  let cards = [];

  this.getCards = () => (cards);

  this.setCards = (newCards) => {
    cards = newCards;
  };

  this.clearCards = () => {
    cards = [];
  };

  this.addCard = () => { };

  this.removeCard = () => { };

  this.updateCard = () => { };
};
